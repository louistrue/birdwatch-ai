# Database Configuration
DB_PASSWORD=birdwatch123

# Frigate Configuration
FRIGATE_RTSP_PASSWORD=password

# Camera Configuration (update with your camera details)
CAMERA_IP=192.168.1.100
CAMERA_USER=admin
CAMERA_PASSWORD=your_camera_password
CAMERA_RTSP_MAIN=rtsp://${CAMERA_USER}:${CAMERA_PASSWORD}@${CAMERA_IP}:554/stream1
CAMERA_RTSP_SUB=rtsp://${CAMERA_USER}:${CAMERA_PASSWORD}@${CAMERA_IP}:554/stream2

# Classification Settings
CONFIDENCE_THRESHOLD=0.6
CORRELATION_WINDOW=30

# Optional: BirdNET Integration
BIRDNET_URL=http://localhost:8000
